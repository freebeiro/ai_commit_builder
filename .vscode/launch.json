{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Python: Commit Tool",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/commit_generator/generate_commit.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "justMyCode": true
        }
    ]
}
